Steps:

Parsing:

1. Lexical analysis: Breaking down input strings into tokens, which
are words that are of a specific type.

2. Parsing: Organizing tokens into a struct representing each command.
That is, having each command be associated to each of its parameters inside a struct.

3. Handling quotes: quotes determine the token boundaries.
"$X" is interpreted as a string containing X's value
and '$X' is interpreted as the string $X.

4. Identifying operators like pipes and redirections

5. 

For the ENVs, we'll use a linked list that we'll return to a char **env whenever needed.
For Export, we'll print all elements of the list, including those with value == NULL. 
For Env, we'll print all elements except those that have value == NULL. Note that "" != NULL.